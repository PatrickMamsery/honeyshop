import 'package:get/get.dart';

class PrivacyController extends GetxController {
  // final MyRepository repository;
  // PrivacyController(this.repository);

  // final _obj = ''.obs;
  // set obj(value) => this._obj.value = value;
  // get obj => this._obj.value;
}
